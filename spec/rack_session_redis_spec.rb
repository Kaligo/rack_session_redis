# frozen_string_literal: true

RSpec.describe RackSessionRedis do
  it "has a version number" do
    expect(RackSessionRedis::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
